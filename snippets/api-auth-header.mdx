All cf0 API requests require a Bearer token in the `Authorization` header:

```
Authorization: Bearer <your-token>
```

Tokens are short-lived Clerk session JWTs scoped to your active organisation. See [Authentication](/api/authentication) for how to obtain and refresh them.
